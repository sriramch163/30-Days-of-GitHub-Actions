name: Reusable Workflow

on:
  workflow_call:
    inputs:
      message:
        required: true
        type: string

jobs:
  reusable-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Message from caller: ${{ inputs.message }}"